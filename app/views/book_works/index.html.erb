<h1>Bookwork</h1>

<%= link_to "Create New Item", new_book_work_path %>

<% @bookWorks.each do |bookwork|%>
  <p><%= bookwork.title %></p>
  <p><%= bookwork.subtitle %></p>
  <p><%= bookwork.body %></p>
  <%= image_tag bookwork.thumb_image unless bookwork.thumb_image.nil? %>
  <%= link_to "Edit", edit_book_work_path(bookwork) %>
<% end %>
